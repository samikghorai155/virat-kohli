<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Advanced HTML & CSS Dashboard</title>

  <!-- =====================
       HARD HTML & CSS ONLY PROJECT
       Features:
       - CSS Grid dashboard layout
       - Dark/Light mode (NO JavaScript)
       - Animated sidebar
       - Pure CSS modal
       - Progress charts using CSS
       - Responsive design
       ===================== -->

  <style>
    /* ===== CSS RESET ===== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", sans-serif;
    }

    :root {
      --bg: #0f172a;
      --card: #020617;
      --text: #e5e7eb;
      --muted: #94a3b8;
      --primary: #38bdf8;
      --accent: #22c55e;
    }

    body {
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }

    /* ===== LIGHT MODE (PURE CSS) ===== */
    #theme:checked ~ .app {
      --bg: #f8fafc;
      --card: #ffffff;
      --text: #020617;
      --muted: #475569;
      --primary: #2563eb;
      --accent: #16a34a;
    }

    /* ===== APP LAYOUT ===== */
    .app {
      display: grid;
      grid-template-columns: 260px 1fr;
      min-height: 100vh;
      background: var(--bg);
      transition: background 0.4s ease;
    }

    /* ===== SIDEBAR ===== */
    .sidebar {
      background: linear-gradient(180deg, var(--card), #020617);
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 20px;
      animation: slideIn 1s ease;
    }

    @keyframes slideIn {
      from { transform: translateX(-100%); }
      to { transform: translateX(0); }
    }

    .logo {
      font-size: 1.4rem;
      font-weight: bold;
      color: var(--primary);
    }

    .menu a {
      display: block;
      padding: 12px 14px;
      border-radius: 10px;
      color: var(--text);
      text-decoration: none;
      transition: 0.3s;
    }

    .menu a:hover {
      background: rgba(56,189,248,0.15);
      transform: translateX(5px);
    }

    /* ===== MAIN CONTENT ===== */
    .main {
      padding: 25px;
      display: grid;
      grid-template-rows: auto 1fr;
      gap: 20px;
    }

    /* ===== HEADER ===== */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header h1 {
      font-size: 1.6rem;
    }

    .toggle {
      display: flex;
      align-items: center;
      gap: 10px;
      cursor: pointer;
    }

    .switch {
      width: 50px;
      height: 26px;
      background: #334155;
      border-radius: 30px;
      position: relative;
    }

    .switch::after {
      content: "";
      width: 22px;
      height: 22px;
      background: white;
      border-radius: 50%;
      position: absolute;
      top: 2px;
      left: 2px;
      transition: 0.3s;
    }

    #theme:checked ~ .app .switch::after {
      transform: translateX(24px);
    }

    /* ===== DASHBOARD GRID ===== */
    .dashboard {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .card {
      background: var(--card);
      border-radius: 18px;
      padding: 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    .card h3 {
      margin-bottom: 15px;
      font-size: 1.1rem;
    }

    /* ===== PROGRESS RING (PURE CSS) ===== */
    .ring {
      --value: 72;
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background: conic-gradient(var(--primary) calc(var(--value) * 1%), #1e293b 0);
      display: grid;
      place-items: center;
      margin: auto;
    }

    .ring span {
      background: var(--card);
      width: 85px;
      height: 85px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      font-weight: bold;
    }

    /* ===== MODAL (NO JS) ===== */
    .modal-toggle {
      display: none;
    }

    .modal {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.6);
      display: grid;
      place-items: center;
      opacity: 0;
      pointer-events: none;
      transition: 0.4s;
    }

    .modal-box {
      background: var(--card);
      padding: 30px;
      border-radius: 20px;
      width: 90%;
      max-width: 400px;
      text-align: center;
      transform: scale(0.7);
      transition: 0.4s;
    }

    .modal-toggle:checked + .modal {
      opacity: 1;
      pointer-events: auto;
    }

    .modal-toggle:checked + .modal .modal-box {
      transform: scale(1);
    }

    .btn {
      display: inline-block;
      padding: 10px 18px;
      border-radius: 10px;
      background: var(--primary);
      color: white;
      text-decoration: none;
      margin-top: 15px;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      .app {
        grid-template-columns: 1fr;
      }
      .sidebar {
        flex-direction: row;
        overflow-x: auto;
      }
    }
  </style>
</head>
<body>

  <!-- THEME SWITCH (NO JS) -->
  <input type="checkbox" id="theme" hidden />

  <div class="app">
    <!-- SIDEBAR -->
    <aside class="sidebar">
      <div class="logo">⚡ Dashboard</div>
      <nav class="menu">
        <a href="#">Overview</a>
        <a href="#">Analytics</a>
        <a href="#">Projects</a>
        <a href="#">Settings</a>
      </nav>
    </aside>

    <!-- MAIN -->
    <main class="main">
      <div class="header">
        <h1>Advanced HTML & CSS Project</h1>
        <label for="theme" class="toggle">
          <span>🌙 / ☀️</span>
          <div class="switch"></div>
        </label>
      </div>

      <section class="dashboard">
        <div class="card">
          <h3>Performance</h3>
          <div class="ring" style="--value:82"><span>82%</span></div>
        </div>

        <div class="card">
          <h3>Users Growth</h3>
          <div class="ring" style="--value:64"><span>64%</span></div>
        </div>

        <div class="card">
          <h3>Pure CSS Modal</h3>
          <label for="open" class="btn">Open Modal</label>
        </div>
      </section>
    </main>
  </div>

  <!-- MODAL STRUCTURE -->
  <input type="checkbox" id="open" class="modal-toggle" />
  <div class="modal">
    <div class="modal-box">
      <h2>🎉 No JavaScript!</h2>
      <p>This modal, theme switch, animations and charts are built using only HTML & CSS.</p>
      <label for="open" class="btn">Close</label>
    </div>
  </div>

</body>
</html>
